{
  "name": "bunrise",
  "module": "./out/index.js",
  "main": "./out/index.js",
  "types": "./types/index.d.ts",
  "version": "0.1.0-alpha.20",
  "description": "lightweight and flexible front-end library based on Bun.js for modern web apps",
  "license": "MIT",
  "type": "module",
  "author": {
    "name": "Aral Roca Gòmez",
    "email": "contact@aralroca.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/aralroca/bunrise.git"
  },
  "files": [
    "out",
    "jsx-runtime",
    "jsx-dev-runtime",
    "types"
  ],
  "scripts": {
    "build": "bun run clean && bun run build:jsx-runtime && bun run build:jsx-dev-runtime && bun run build:bunrise",
    "build:bunrise": "bun build --minify --target=bun --outdir=out packages/bunrise/index.ts",
    "build:jsx-runtime": "bun build --minify --target=bun --outdir=jsx-runtime packages/jsx-runtime/index.ts && cp types/index.d.ts jsx-runtime/index.d.ts",
    "build:jsx-dev-runtime": "bun build --minify --target=bun --outdir=jsx-dev-runtime packages/jsx-runtime/index.ts && cp types/index.d.ts jsx-dev-runtime/index.d.ts",
    "test": "bun test",
    "release:alpha": "bun run build && npm publish --tag next && bun run clean",
    "clean": "rm -rf out jsx-runtime jsx-dev-runtime"
  },
  "devDependencies": {
    "bun-types": "latest",
    "bunrise": "0.1.0-alpha.20"
  },
  "peerDependencies": {
    "typescript": "5.0.0"
  }
}