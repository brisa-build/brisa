{
  "name": "bunrise",
  "module": "./out/index.js",
  "main": "./out/index.js",
  "version": "0.1.0-alpha.11",
  "description": "lightweight and flexible front-end library based on Bun.js for modern web apps",
  "license": "MIT",
  "type": "module",
  "author": {
    "name": "Aral Roca Gòmez",
    "email": "contact@aralroca.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/aralroca/bunrise.git"
  },
  "files": [
    "out",
    "jsx-runtime",
    "jsx-dev-runtime"
  ],
  "scripts": {
    "build": "bun run build:jsx-runtime && bun run build:jsx-dev-runtime && bun run build:bunrise",
    "build:bunrise": "bun build --minify --target=bun --outdir=out bunrise/index.ts",
    "build:jsx-runtime": "bun build --minify --target=bun --outdir=jsx-runtime jsx-runtime/index.ts",
    "build:jsx-dev-runtime": "bun build --minify --target=bun --outdir=jsx-dev-runtime jsx-runtime/index.ts",
    "test": "bun test",
    "release:alpha": "bun run build && npm publish --tag next"
  },
  "devDependencies": {
    "bun-types": "latest",
    "bunrise": "0.1.0-alpha.7"
  },
  "peerDependencies": {
    "typescript": "5.0.0"
  }
}