{
  "name": "bunrise",
  "module": "./out/bunrise/index.js",
  "main": "./out/bunrise/index.js",
  "types": "./out/bunrise/index.d.ts",
  "version": "0.1.0-alpha.40",
  "description": "lightweight and flexible front-end library based on Bun.js for modern web apps",
  "license": "MIT",
  "type": "module",
  "author": {
    "name": "Aral Roca Gòmez",
    "email": "contact@aralroca.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/aralroca/bunrise.git"
  },
  "files": [
    "out",
    "jsx-runtime",
    "jsx-dev-runtime",
    "cli.sh"
  ],
  "scripts": {
    "build": "bun run clean && bun run build:jsx-runtime && bun run build:jsx-dev-runtime && bun run build:bunrise",
    "build:bunrise": "bun build --minify --target=bun --outdir=out src/bunrise/* src/cli/* && cp src/types/index.d.ts out/bunrise/index.d.ts",
    "build:jsx-runtime": "bun build --minify --target=bun --outdir=jsx-runtime src/jsx-runtime/index.ts && cp src/types/index.d.ts jsx-runtime/index.d.ts",
    "build:jsx-dev-runtime": "bun build --minify --target=bun --outdir=jsx-dev-runtime src/jsx-runtime/index.ts && cp src/types/index.d.ts jsx-dev-runtime/index.d.ts",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "release:alpha": "bun run build && npm publish --tag next && bun run clean",
    "clean": "rm -rf out jsx-runtime jsx-dev-runtime cli",
    "format": "bunx prettier --write ."
  },
  "bin": {
    "bunrise": "./cli.sh"
  },
  "devDependencies": {
    "bun-types": "latest",
    "bunrise": "0.1.0-alpha.39"
  },
  "peerDependencies": {
    "typescript": "5.0.0"
  }
}